---
- name: Generate Initial SSH Setup Inventory
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Generate initial ssh setup inventory file
      ansible.builtin.template:
        src: ../templates/initial_ssh_setup_inventory.j2
        dest: ansible/inventory.ini
        mode: '0644'
